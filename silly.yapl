class Silly {
    test: Int <- 1 + 2;
    a: Int <- 3;
    copy() : SELF_TYPE { self };

};

class Sally inherits Silly {};

class Main {
    x : Silly <- (new Silly).copy();
    x2: Sally;

    main() : Silly { x };
    otromain(var1: Int, var2: Int): Int {
        var1 + var2
    };
};